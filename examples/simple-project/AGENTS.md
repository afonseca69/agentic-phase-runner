# Agent Instructions

This example is public-safe and provider-agnostic.

Rules:

- Do not read or print secrets.
- Do not use real external APIs.
- Do not run destructive commands.
- Execute only the requested phase.
- Report files changed, commands run, validation steps, risks, and pending work.
